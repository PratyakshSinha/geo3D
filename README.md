# geo3d

A 3D geometry library built in C++ from scratch.

## Features
- Vec3: 3D vector with dot product, cross product, normalization

## Building
Requires CMake 3.20+ and MSVC

```bash
mkdir build && cd build
cmake ..
cmake --build .
```